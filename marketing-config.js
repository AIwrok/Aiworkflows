// Paste your IDs after signing up for each service. Leave blank to keep that tool off.
window.AI_WORKFLOWS_MARKETING = {
  // Google Tag Manager — recommended hub for all tags (analytics, ads, pixels)
  GTM_CONTAINER_ID: "",

  // Google Analytics 4 — traffic, sources, pages, conversions
  // Use this OR GTM (if GTM is set, GA4 is usually added inside GTM instead)
  GA4_MEASUREMENT_ID: "",

  // Meta Pixel — Facebook / Instagram ads + retargeting
  META_PIXEL_ID: "",

  // Microsoft Clarity — free heatmaps & session recordings
  CLARITY_PROJECT_ID: "",

  // Google AdSense — show ads after approval (monetize blog/traffic pages)
  ADSENSE_CLIENT_ID: "",
  ADSENSE_SLOT_ID: ""
};
